# PostgreSQL credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgrespassword
POSTGRES_DB=pedidos

# Conexión a la base de datos (aplicable al backend)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=${POSTGRES_DB}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}

# Frontend (build-time)
VITE_API_URL=/api

# Backend configuración
APP_PORT=8080
